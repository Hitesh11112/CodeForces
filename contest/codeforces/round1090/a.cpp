#include <bits/stdc++.h>
using namespace std;

int main() {
  long long t;
  cin >> t;
  while (t--) {
    long long num;
    cin >> num;
    if (num >= 0) {
      cout << num + 1 << '\n';
    } else {
      cout << num + 1 << '\n';

  }
  return 0;
}